#include "main.h"

/**
 * mul -> multiplies two integers
 * @a: a character argument
 * @b: b character argument
 * Return: return 1 or 0 depending on the condition
 */
int mul(int a, int b)
{
	return (a * b);
}
