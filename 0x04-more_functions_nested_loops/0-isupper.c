#include "main.h"
/**
 * _isupper -> check the character is uppercase
 * @c : a character argument 
 * Return: return 1 or 0 depending on the condition
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
